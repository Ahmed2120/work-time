// backup_db_helper.dart
// NOTE: Manual local backup is deprecated. Cloud backup via Google Drive is now
// the primary mechanism (see google_drive_service.dart & backup_view.dart).
// This file is kept as a stub to avoid breaking any remaining references.

class BackupHelper {
  // Legacy manual backup — no longer used in production.
  // All backup operations now go through GoogleDriveService.
}
